import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AboutOmegaComponent } from './about-omega.component';

describe('AboutOmegaComponent', () => {
  let component: AboutOmegaComponent;
  let fixture: ComponentFixture<AboutOmegaComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AboutOmegaComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AboutOmegaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
